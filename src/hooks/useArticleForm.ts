
import { useArticleForm as useArticleFormImplementation } from './article/useArticleForm';

/**
 * Hook for managing article form state and operations
 * This is a re-export of the implementation to maintain API compatibility
 */
export const useArticleForm = useArticleFormImplementation;
